# Shared

Shared types and utilities between client and server.

## Usage

Import types from this folder in both frontend and backend projects:

```typescript
import { UserRole, OrderStatus } from '@shared/types'
```
